---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';

const title = "AI 能力边界";
const description = "AI 编程工具能做什么、不能做什么——何时相信它、何时叫停它";
---

<MainLayout title={title} description={description}>
  <Sidebar currentPath="/research/ai-boundaries" slot="sidebar" />
  <h1>{title}</h1>
  <div class="meta-line">
    <span class="tag">理论</span>
    <span class="tag">Foundations</span>
  </div>

  <blockquote>
    <p>知道 AI 的边界，比知道它的能力更重要——因为边界是你决策的依据。</p>
  </blockquote>

  <h2 id="why-boundaries-matter">为什么边界比能力重要</h2>
  <p>团队在引入 AI 编程时最常见的错误不是低估 AI，而是<strong>高估 AI 的可靠性边界</strong>——把 AI 能做到的事情当成 AI 总是能做到的事情。</p>
  <p>AI 在熟悉领域表现好，不等于它在所有领域都好。在边界内 AI 是工具，在边界外 AI 是风险放大器。</p>

  <h2 id="what-ai-can-do">AI 真正擅长的</h2>

  <table>
    <thead><tr><th>任务类型</th><th>为什么 AI 行</th><th>前提条件</th></tr></thead>
    <tbody>
      <tr><td>语法转换</td><td>有明确规则，可以穷举</td><td>输入输出格式清晰</td></tr>
      <tr><td>批量重命名 / 规范化</td><td>模式匹配，不需要业务判断</td><td>不会影响业务逻辑</td></tr>
      <tr><td>生成测试用例</td><td>给定输入输出，输出用例</td><td>人先确认功能正确性</td></tr>
      <tr><td>写文档 / 注释</td><td>已有内容，归纳总结</td><td>代码本身是正确的</td></tr>
      <tr><td>已知模式的代码实现</td><td>见过的模式，AI 能复现</td><td>业务逻辑已在 SPEC 里</td></tr>
      <tr><td>单函数重构</td><td>边界清晰，验证容易</td><td>不超过 50 行，有单元测试</td></tr>
      <tr><td>代码补全 / 模板填充</td><td>上下文充足，答案唯一</td><td>上下文在 token 限制内</td></tr>
      <tr><td>静态分析 + 自动修复</td><td>规则明确，结果可验证</td><td>lint 规则已经定义好</td></tr>
    </tbody>
  </table>

  <h2 id="what-ai-cannot-do">AI 结构性做不到的</h2>

  <h3>1. 不知道自己做错了</h3>
  <p>AI 没有自我纠错的能力——它的输出质量取决于输入质量和模型状态，同一个任务可能这次对下次错，它自己不知道。</p>
  <p><strong>后果</strong>：在长链条任务中，AI 会在错误的方向上继续构建，越到后面成本越高。</p>
  <p><strong>对策</strong>：拆分任务，每步验证后再继续。</p>

  <h3>2. 没有业务上下文记忆</h3>
  <p>AI 的上下文窗口有限，跨长对话或大代码库时，AI 会遗忘最早的信息，导致前后不一致。</p>
  <p><strong>后果</strong>：开头和结尾的代码风格、逻辑、数据结构不一致。</p>
  <p><strong>对策</strong>：每个任务不超过 2 小时，大任务拆成多个 SPEC。</p>

  <h3>3. 不理解隐含业务规则</h3>
  <p>代码里有很多"隐含契约"：某个字段为什么不能为空、某个 API 为什么要先调另一个 API、某个时间戳为什么要用 UTC。这些不在代码里，AI 读不出来。</p>
  <p><strong>后果</strong>：AI 改的代码能跑测试，但在特定边界条件下触发历史 bug。</p>
  <p><strong>对策</strong>：SPEC 里必须包含隐含约束的说明，AI 必须读懂这些才能动。</p>

  <h3>4. 不能验证自己输出的正确性</h3>
  <p>AI 能生成代码，但生成出来的代码是否解决了问题，AI 自己不会主动验证——除非你明确要求它验证。</p>
  <p><strong>后果</strong>：AI 自信地完成了一个错误的任务，并报告 done。</p>
  <p><strong>对策</strong>：每个任务的验收条件必须在 SPEC 里写清楚，AI 完成后要对照 SPEC 自验。</p>

  <h3>5. 在跨系统边界上必然踩坑</h3>
  <p>跨第三方 API、跨数据库、跨微服务的逻辑，AI 只能在文档层面理解，无法知道实际的行为细节。</p>
  <p><strong>后果</strong>：迁移接口时，AI 假设两边行为一致，但实际往往有细微差异。</p>
  <p><strong>对策</strong>：跨系统边界任务必须由人先做充分调研，AI 只能在人的框架内执行。</p>

  <h3>6. 不知道自己的知识截止日期</h3>
  <p>AI 的训练数据有截止日期，它不知道哪些是新知识、哪些已经变了。</p>
  <p><strong>后果</strong>：在新版本框架、新版 SDK 上给出过时的建议。</p>
  <p><strong>对策</strong>：框架/SDK 版本信息必须在上下文里明确给出。</p>

  <h2 id="context-matters">上下文决定边界</h2>
  <p>AI 能力的边界不是固定的——同一个模型，在不同上下文下表现差异巨大：</p>

  <table>
    <thead><tr><th>上下文质量</th><th>AI 实际表现</th></tr></thead>
    <tbody>
      <tr><td>上下文精确、边界清晰、有完整 SPEC</td><td>接近中级工程师水平</td></tr>
      <tr><td>上下文模糊、只有目标描述</td><td>像一个很聪明但不了解情况的新人</td></tr>
      <tr><td>上下文有过时信息</td><td>比没有上下文还危险（AI 会基于错误信息推理）</td></tr>
      <tr><td>上下文超载（喂了太多代码）</td><td>能力急剧下降，前后的建议互相矛盾</td></tr>
    </tbody>
  </table>

  <h2 id="task-decomposition">任务拆分的颗粒度原则</h2>
  <p>AI 能力的边界决定了任务拆分的颗粒度。以下原则：</p>

  <h3>时间原则</h3>
  <p>单个 AI 任务不超过 2 小时。如果预计超过 2 小时，拆。</p>

  <h3>验证原则</h3>
  <p>每个任务必须能在一个明确的验证步骤里确认完成。无法验证的任务不能让 AI 独立完成。</p>

  <h3>边界原则</h3>
  <p>跨系统、跨团队、跨第三方依赖的任务，AI 只能执行人已经拆好的子任务，不能自己规划整个迁移。</p>

  <h3>上下文原则</h3>
  <p>AI 需要读的代码量如果超过 2000 行，考虑先让人做摘要，AI 只读摘要。上下文越少，AI 越准。</p>

  <h2 id="confidence-levels">任务信心等级</h2>
  <p>在派任务给 AI 之前，先评估信心等级：</p>

  <table>
    <thead><tr><th>等级</th><th>任务特征</th><th>建议</th></tr></thead>
    <tbody>
      <tr><td>🟢 高信心</td><td>边界清晰、验证明确、熟悉领域、有 SPEC</td><td>可以直接派 AI 执行，小 review</td></tr>
      <tr><td>🟡 中信心</td><td>有一定边界，但局部不明确</td><td>AI 执行，关键节点人 review</td></tr>
      <tr><td>🟠 低信心</td><td>跨系统、有历史包袱、边界模糊</td><td>人做框架，AI 只执行确定的子任务</td></tr>
      <tr><td>🔴 禁区</td><td>安全关键、支付相关、权限变更、数据迁移</td><td>AI 辅助，人全程掌控</td></tr>
    </tbody>
  </table>

  <div class="related-links">
    <h3>相关章节</h3>
    <ul>
      <li><a href="/handbook/ai-as-amplifier">AI 是放大器不是工程师</a></li>
      <li><a href="/handbook/anti-patterns">反模式</a></li>
      <li><a href="/handbook/spec-format">Spec 字段规范</a></li>
      <li><a href="/handbook/workflow">6 步工作流</a></li>
    </ul>
  </div>
</MainLayout>

<style>
  .related-links {
    margin-top: 3rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border-color, #e5e7eb);
  }
  .related-links ul {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    list-style: none;
    padding: 0;
  }
  .related-links li a {
    font-size: 0.875rem;
  }
</style>
</MainLayout>