---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="Coding Agent 生态全景" description="2026 年主流 AI Coding Agent 的架构设计、能力对比和选型指南：从 Devin 到 Claude Code">
  <Sidebar currentPath="/research/agent-ecosystem" slot="sidebar" />
  <h1>Coding Agent 生态全景</h1>
  <div class="meta-line"><span class="tag">工具调研</span> 2026 年 7 月</div>

  <h2 id="overview">概述</h2>
  <p>2026 年的 Coding Agent 已经从概念验证走向生产使用。市场上形成了<strong>三种架构范式</strong>，每种有不同的适用场景和设计哲学。</p>

  <h2 id="three-paradigms">三种架构范式</h2>
  <table>
    <thead><tr><th>范式</th><th>代表产品</th><th>核心理念</th><th>人机关系</th></tr></thead>
    <tbody>
      <tr><td><strong>IDE 嵌入式</strong></td><td>Cursor Agent、Windsurf、Copilot Agent</td><td>AI 是你的副驾驶，在你编辑器中实时协作</td><td>紧密协作，人随时可见、可干预</td></tr>
      <tr><td><strong>终端 Agent</strong></td><td>Claude Code、Codex CLI、Gemini CLI</td><td>AI 是你的终端操作员，接收指令、执行任务、回报结果</td><td>分配任务-等待结果，中间有信任间隙</td></tr>
      <tr><td><strong>云端自治 Agent</strong></td><td>Devin、Factory、Cosine、OpenHands</td><td>AI 是独立工程师，接受 PR/Issue，在沙箱中自主完成</td><td>异步协作，人来 Review 产出</td></tr>
    </tbody>
  </table>

  <h2 id="comparison">主流 Agent 深度对比</h2>
  <table>
    <thead><tr><th>产品</th><th>架构</th><th>核心能力</th><th>已知盲区</th><th>SWE-bench</th></tr></thead>
    <tbody>
      <tr>
        <td><strong>Claude Code</strong></td>
        <td>终端 Agent</td>
        <td>超长上下文理解、自主文件编辑、Shell 集成、MCP 工具链</td>
        <td>大项目时上下文管理困难、有时过度自信做出破坏性修改</td>
        <td>~49% (GPT-5.2)</td>
      </tr>
      <tr>
        <td><strong>Cursor Agent</strong></td>
        <td>IDE 嵌入式</td>
        <td>实时代码理解、Composer 多文件编辑、terminal 集成</td>
        <td>长任务时上下文漂移、Agent 模式依赖模型质量</td>
        <td>取决于后端模型</td>
      </tr>
      <tr>
        <td><strong>Codex CLI</strong></td>
        <td>终端 Agent</td>
        <td>OpenAI 官方、沙箱执行、Plan-Execute 模式</td>
        <td>生态成熟度不及 Claude Code、MCP 支持较晚</td>
        <td>取决于后端模型</td>
      </tr>
      <tr>
        <td><strong>Devin</strong></td>
        <td>云端自治</td>
        <td>完整 dev 环境、自带浏览器、PR-driven 工作流</td>
        <td>成本高、反馈周期长、环境限制多</td>
        <td>~14% (自报)</td>
      </tr>
      <tr>
        <td><strong>OpenHands</strong></td>
        <td>云端自治(开源)</td>
        <td>完全开源、Docker 沙箱、插件体系</td>
        <td>社区驱动、质量不稳定、需要自己搭 infra</td>
        <td>~26% (SWE-agent)</td>
      </tr>
      <tr>
        <td><strong>Gemini CLI</strong></td>
        <td>终端 Agent</td>
        <td>Google 生态集成、多模态理解、免费额度</td>
        <td>对非 GCP 项目支持较弱、上下文窗口限制</td>
        <td>~30%</td>
      </tr>
    </tbody>
  </table>

  <h2 id="architecture-patterns">Agent 架构模式</h2>
  <h3>模式 1：Plan-Execute（规划-执行）</h3>
  <p>Codex CLI 和 OpenHands 采用此模式。Agent 先生成执行计划（Plan），经过确认后逐步执行（Execute）。</p>
  <ul>
    <li><strong>优点</strong>：人类可以在计划阶段纠正方向，避免 Agent 走冤枉路</li>
    <li><strong>缺点</strong>：计划阶段本身需要时间，简单任务显得过度设计</li>
  </ul>

  <h3>模式 2：ReAct Loop（推理-行动循环）</h3>
  <p>Claude Code 和 Cursor Agent 采用此模式。Agent 思考一步、执行一步、观察结果、再思考下一步。</p>
  <ul>
    <li><strong>优点</strong>：灵活，能处理意外情况</li>
    <li><strong>缺点</strong>：长任务时容易陷入循环或「迷路」</li>
  </ul>

  <h3>模式 3：Multi-Agent（多 Agent 协作）</h3>
  <p>Google Agent Bake-Off 验证的方向。一个「规划师」Agent 分解任务，一个「编码」Agent 写代码，一个「审查」Agent 检查质量。</p>
  <ul>
    <li><strong>优点</strong>：避免单一 Agent 的认知过载，质量更高</li>
    <li><strong>缺点</strong>：复杂度高、成本高、调试困难</li>
  </ul>

  <h2 id="mcp">MCP（Model Context Protocol）—— Agent 的工具标准</h2>
  <p>Anthropic 2024 年底提出的 MCP 协议已成为 Agent 工具接入的事实标准。Shopify、Cloudflare、Vercel 等平台都已发布 MCP Server。</p>
  <blockquote>
    <p>MCP 之于 Agent，就像 USB 之于外设——让 Agent 能「即插即用」地接入各种工具。</p>
  </blockquote>
  <p>关键影响：MCP 让 Agent 能做的事情从「读文件、写代码」扩展到「操作数据库、管理部署、查询 API」。</p>

  <h2 id="selection">选型决策框架</h2>
  <table>
    <thead><tr><th>如果你需要</th><th>推荐</th></tr></thead>
    <tbody>
      <tr><td>日常编码辅助、实时代码理解</td><td>Cursor Agent + Claude 后端</td></tr>
      <tr><td>复杂多文件重构、大范围修改</td><td>Claude Code（终端 Agent 模式）</td></tr>
      <tr><td>自动化 Issue → PR 流水线</td><td>Devin 或自建 OpenHands</td></tr>
      <tr><td>零成本探索、开源可控</td><td>OpenHands + 本地模型</td></tr>
      <tr><td>Google Cloud 生态内项目</td><td>Gemini CLI</td></tr>
      <tr><td>Shopify App 开发</td><td>Shopify AI Toolkit (MCP)</td></tr>
    </tbody>
  </table>

  <h2 id="lessons">从 Agent 使用中学到的 5 个教训</h2>
  <ol>
    <li><strong>Agent 需要护栏，不是「放养」。</strong> 给它明确的边界——可以改哪些文件、不能碰哪些目录、需要人工确认的操作类型。</li>
    <li><strong>AGENTS.md 是 Agent 的「入职手册」。</strong> 在项目根目录创建 AGENTS.md，写清楚项目结构、编码规范、命名约定、测试策略。Agent 会读它。</li>
    <li><strong>Agent 对大项目的理解有递减效应。</strong> 超过 5 万行代码的项目，不要让 Agent 一次性理解全部——拆分任务、提供聚焦上下文。</li>
    <li><strong>Agent 写的代码必须经过 review。</strong> 不需要比人写的代码更宽松。有时候甚至应该更严——Agent 容易生成「看起来合理但逻辑有偏差」的代码。</li>
    <li><strong>经验越丰富的开发者从 Agent 中获益越多。</strong> METR 研究（2025 年中）发现，初级开发者用 AI 后可能更慢——因为他们不知道 AI 错在哪。</li>
  </ol>
</MainLayout>
