---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="OpenSpec 生产环境调优" description="OpenSpec / Spec Kit / BMAD 等 SDD 工具默认配置往往不足以支撑企业落地；调优维度包括自定义 schema、validation 钩子、模板库、流程集成、AI 指令优化与反模式">
  <Sidebar currentPath="/research/openspec-in-production" slot="sidebar" />
  <h1>OpenSpec 生产环境调优</h1>
  <div class="meta-line">
    <span class="tag">工具调优</span>
    <span class="tag">生产落地</span>
  </div>

  <blockquote>
    <p>OpenSpec 默认配置是个人 / 小团队友好的。<strong>企业落地时，调优才是真正的工程</strong>——把通用 SDD 工具配置成你企业实际需要的形态。</p>
  </blockquote>

  <h2 id="one-sentence">一句话总结</h2>
  <p>SDD 工具（OpenSpec / Spec Kit / BMAD）的默认 schema 是通用模板。<strong>生产环境需要在这五个维度上调优</strong>：自定义 schema、validation 钩子、模板库、流程集成、AI 指令调优。每一项都有具体的"配什么"和"避免什么"。</p>

  <h2 id="why-tune">为什么必须调优</h2>
  <p>默认配置面临三个错位：</p>

  <ul>
    <li><strong>通用 vs 企业</strong>：OpenSpec 默认 schema 不包含你的合规字段（GDPR / 等保 / 数据出境）；Spec Kit 的 phases 是 GitHub 风格，<strong>你企业有"反串讲"</strong>这种阶段。</li>
    <li><strong>工具 vs 流程</strong>：CLI 校验语法 OK，但<strong>评审会议开法、合规审计、归档触发</strong>不在工具内置。</li>
    <li><strong>单人 vs 团队</strong>：默认配置假定一人一 spec，<strong>企业里并行分支 / 跨团队 sync / 多 reviewer</strong> 没预设。</li>
  </ul>

  <p>结果就是：工具上线三个月后，团队绕开它回到 IM / Word / Excel，spec 与流程脱节、逐渐失效。</p>

  <h2 id="dimensions">5 个调优维度</h2>

  <table>
    <thead><tr><th>维度</th><th>做什么</th><th>OpenSpec 配置点</th><th>Spec Kit 配置点</th></tr></thead>
    <tbody>
      <tr><td><strong>1. 自定义 schema</strong></td><td>在通用 schema 上加企业必填字段</td><td><code>openspec/schemas/&lt;name&gt;.yaml</code></td><td>fork templates / specs/</td></tr>
      <tr><td><strong>2. validation 钩子</strong></td><td>把 <code>validate</code> 接入 CI / pre-commit</td><td><code>openspec validate --strict</code></td><td>GitHub Action</td></tr>
      <tr><td><strong>3. 模板库</strong></td><td>维护企业内部 spec 模板</td><td>template files / specs 目录</td><td>.specify/templates/</td></tr>
      <tr><td><strong>4. 流程集成</strong></td><td>接 Linear / Jira / 飞书 / Slack 通知</td><td>webhooks / change hooks</td><td>GitHub Issues / Discussions</td></tr>
      <tr><td><strong>5. AI 指令调优</strong></td><td>让 AI 用对的姿势写 spec / lint</td><td>AGENTS.md / system prompt</td><td>copilot-instructions.md</td></tr>
    </tbody>
  </table>

  <h2 id="custom-schema">维度 1：自定义 schema</h2>
  <p>OpenSpec 支持自定义 schema 文件，把企业必填字段编码进去。</p>

  <h3 id="schema-example">示例：合规 + 审计扩展</h3>
  <pre><code class="language-yaml"># openspec/schemas/enterprise-cn.yaml
extends: spec-driven
required_sections:
  - Compliance
  - AuditTrail
  - RollbackPlan
  - RiskAssessment

compliance:
  required: true
  options: [GDPR, 等保二级, 等保三级, 数据出境, 无]
audit:
  required: true
  fields:
    - 操作人
    - 时间
    - 操作对象
    - 结果
rollback:
  required: true
  trigger_conditions_required: true</code></pre>

  <p>这样跑 <code>openspec validate --schema enterprise-cn</code> 时，<strong>没有 Compliance / AuditTrail / RollbackPlan 的 spec 会失败</strong>——CI 直接卡住。</p>

  <h3 id="schema-tips">调优建议</h3>
  <ul>
    <li><strong>从极简开始</strong>：第一版只加 1-2 个必填字段，避免团队抵触。第二轮再加。</li>
    <li><strong>字段名稳定</strong>：Compliance 字段不要突然改叫 ComplianceCheck，<strong>会让已经写的 spec 全部失效</strong>。</li>
    <li><strong>允许 escape hatch</strong>：某些场景需要例外，提供 <code>override_reason</code> 字段让人能解释为什么不填，而不是默默绕过。</li>
  </ul>

  <h2 id="validation-hooks">维度 2：validation 钩子</h2>
  <p>spec 校验不应该靠人记——应该<strong>卡在 CI / pre-commit</strong>。</p>

  <h3 id="pre-commit">pre-commit hook</h3>
  <pre><code class="language-yaml"># .pre-commit-config.yaml
repos:
  - repo: local
    hooks:
      - id: openspec-validate
        name: OpenSpec validate
        entry: npx @fission-ai/openspec@latest validate --strict
        language: system
        files: '\.openspec/.*\.md$'
        pass_filenames: false</code></pre>

  <h3 id="github-action">GitHub Action</h3>
  <pre><code class="language-yaml"># .github/workflows/spec-lint.yml
name: Spec Lint
on: [pull_request]
jobs:
  validate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npx @fission-ai/openspec@latest validate --strict --schema enterprise-cn
      - run: npx @fission-ai/openspec@latest show --json | jq '.[] | select(.open_questions | length &gt; 0) | &#123;id, open_questions&#125;'</code></pre>

  <p><strong>第二个 step 关键</strong>：检查有没有未解决的 open_questions——这些是 spec 里的"待补"，没解决不应该 merge。</p>

  <h3 id="ci-block">CI 阻断的标准做法</h3>
  <ul>
    <li>❌ Spec 无 Compliance 字段 → fail</li>
    <li>❌ Spec 有未解决 open_questions → fail</li>
    <li>❌ Spec 引用不存在的 REQ ID → fail</li>
    <li>❌ Archived spec 与当前 spec 冲突 → warn (允许 legacy 兼容)</li>
    <li>✅ 校验通过 → continue</li>
  </ul>

  <h2 id="template-library">维度 3：模板库</h2>
  <p>OpenSpec 默认 spec 模板是通用骨架。<strong>企业内部应该维护一份自己的模板</strong>——既符合 OpenSpec 规范，又带企业特色字段。</p>

  <h3 id="template-pattern">模板分层模式</h3>

  <pre><code>openspec/
├── schemas/
│   ├── default.yaml           # 工具默认
│   └── enterprise-cn.yaml     # 企业定制（extends default）
├── templates/
│   ├── spec.md                # 通用骨架
│   ├── spec-enterprise-cn.md  # 企业骨架（含 Compliance/Audit/Rollback 段）
│   └── spec-feature-flag.md   # feature flag 专项模板
└── profiles/
    ├── internal-tools.yaml    # 内部工具（小功能模板）
    ├── customer-facing.yaml   # 对客功能（含安全/性能字段）
    └── compliance-heavy.yaml  # 合规密集（含数据出境 / 等保）</code></pre>

  <p><strong>profile 是 schema 的子集</strong>——针对不同类型的 feature 启用不同必填字段集合，避免一刀切。</p>

  <h3 id="template-evolution">模板演进</h3>
  <ul>
    <li>第一版：fork 默认模板，加 Compliance 段</li>
    <li>第二版：拆出 profiles（customer-facing 强制安全字段，internal-tools 不强制）</li>
    <li>第三版：模板放进独立 repo（<code>your-org/openspec-templates</code>），跨项目复用</li>
  </ul>

  <h2 id="process-integration">维度 4：流程集成</h2>
  <p>SDD 工具不是孤岛——必须和你已有的工具链对话。</p>

  <h3 id="linear-integration">Linear 集成</h3>
  <p>Linear ticket → OpenSpec change 的映射：</p>
  <ol>
    <li>PM 在 Linear 创建 ticket</li>
    <li>AI agent 检测到 ticket 标签 <code>spec-needed</code></li>
    <li>Agent 自动在 <code>.openspec/changes/&lt;linear-id&gt;/</code> 创建 change proposal</li>
    <li>通过 webhook 双向 sync：OpenSpec archive 时更新 Linear status 为 Done</li>
  </ol>

  <h3 id="feishu-integration">飞书集成（中国团队）</h3>
  <p>飞书消息 → OpenSpec trigger：</p>
  <ul>
    <li>飞书群里贴 PRD → bot 抓取 → 落 <code>.openspec/changes/&lt;slug&gt;/proposal.md</code></li>
    <li>OpenSpec validate 通过 → 飞书群通知"spec 已就绪，请评审"</li>
    <li>archive 完成 → 飞书群通知"@PM spec 已归档，可以进入实现"</li>
  </ul>

  <h3 id="github-integration">GitHub 集成</h3>
  <ul>
    <li>PR linked issue → OpenSpec 必填字段校验</li>
    <li>PR merged → OpenSpec 自动 archive（如果 PR 引用 spec 编号）</li>
    <li>Discussion <code>spec-review</code> 标签 → 触发 review checklist bot</li>
  </ul>

  <h3 id="integration-anti-patterns">集成反模式</h3>
  <ul>
    <li>❌ <strong>每个改动同步到所有工具</strong>——会变成通知噪音。原则：<strong>关键状态变更才通知</strong>（create / archive / blocked），lint warning 不通知。</li>
    <li>❌ <strong>双向同步不带 conflict resolution</strong>——Linear 和 OpenSpec 谁是 source of truth 必须明确。<strong>Linear 是产品视角，OpenSpec 是 spec 视角</strong>——spec 是权威。</li>
    <li>❌ <strong>Webhook 不做幂等</strong>——同一事件触发多次会重复 archive。必须用 event ID 去重。</li>
  </ul>

  <h2 id="ai-tuning">维度 5：AI 指令调优</h2>
  <p>这是<strong>调优中最被低估</strong>的一环——AI 写 spec / lint spec 的姿势完全由你的指令文件决定。</p>

  <h3 id="agents-md">AGENTS.md / copilot-instructions.md</h3>
  <p>在仓库根放一份"AI 怎么用这个 spec 体系"的指令文件。OpenClaw / Cursor / Claude Code 都认这个文件。</p>

  <h3 id="ai-tuning-example">示例片段</h3>
  <pre><code class="language-markdown">## 写 spec 时
- 每个 Requirement 必配 ≥ 1 Scenario（GIVEN/WHEN/THEN）
- 用 SHALL/SHOULD/MAY 标 RFC 2119 强度
- 量化验收口径（不能写"性能好"——要写"P99 &lt; 200ms"）
- 复杂场景拆成多个 Scenario，不要一个 Scenario 包圆

## lint spec 时
- 4 段 Lint：歧义 / 缺失 / 冲突 / 可执行性
- 每个 Lint 一个新会话（上下文污染）
- Lint 输出要可追溯（哪段 spec / 哪个字段 / 什么问题）

## 反杜撰
- 合成 reviewer 意见只能基于用户提供的会注
- 缺 reviewer 意见用 [待补充：需 XX 提供意见] 占位
- 严禁凭空生成 reviewer 发言

## OpenSpec 操作
- `openspec instructions specs --change &lt;id&gt;` 用这个拿结构化指引
- 不要靠模型记忆 schema 细节
- 每次 archive 前必跑 `openspec validate --strict`</code></pre>

  <h3 id="ai-tuning-leverage">指令调优杠杆点</h3>
  <table>
    <thead><tr><th>杠杆</th><th>效果</th></tr></thead>
    <tbody>
      <tr><td>在指令里<strong>列反模式</strong></td><td>比正面说"应该这样"更有效——AI 看到禁止项会警觉</td></tr>
      <tr><td>在指令里<strong>给完整示例</strong></td><td>比抽象规则更有效——便于按样例对齐格式</td></tr>
      <tr><td>指令文件<strong>放在仓库根</strong></td><td>所有人共用同一份指令——团队一致性</td></tr>
      <tr><td>指令里<strong>列必跑命令</strong></td><td>AI 不会跳过 <code>openspec validate</code></td></tr>
      <tr><td>指令<strong>定期更新</strong></td><td>不更新的指令 = 过时的指令；建议每次评审后迭代</td></tr>
    </tbody>
  </table>

  <h2 id="recommended-config">推荐配置清单（最小可用）</h2>
  <p>给一个想落地的团队的"30 天启动"配置：</p>

  <h3 id="week-1">第 1 周：基础 schema + CI</h3>
  <ul>
    <li>fork 默认 schema，加 2-3 个企业必填字段</li>
    <li>pre-commit hook 接 <code>openspec validate --strict</code></li>
    <li>跑 1-2 个 feature 验证</li>
  </ul>

  <h3 id="week-2">第 2 周：模板 + AI 指令</h3>
  <ul>
    <li>建企业内部 <code>spec-template.md</code></li>
    <li>写 AGENTS.md / copilot-instructions.md（核心反杜撰 + 必跑命令）</li>
    <li>新 feature 用新模板试跑</li>
  </ul>

  <h3 id="week-3">第 3 周：流程集成</h3>
  <ul>
    <li>接 Linear / Jira / 飞书的 webhook（至少一边）</li>
    <li>review 决议同步回 ticket 状态</li>
  </ul>

  <h3 id="week-4">第 4 周：profiles + 文档</h3>
  <ul>
    <li>拆 profiles（internal-tools / customer-facing / compliance-heavy）</li>
    <li>写团队内部 onboarding 文档</li>
    <li>retain 3 个 feature 验证</li>
  </ul>

  <h2 id="production-anti-patterns">生产反模式（必看）</h2>

  <h3 id="ap-1">AP-1：把 schema 当作合规审计</h3>
  <p>Schema 校验<strong>只是格式校验</strong>，不是合规审计。一个 spec 通过 <code>validate</code> 不代表它符合 GDPR。要做合规审计，<strong>schema 校验是必要不充分条件</strong>。</p>

  <h3 id="ap-2">AP-2：所有 spec 强求同一份 profile</h3>
  <p>内部工具不该填客户面功能的合规字段。<strong>一刀切 = 阻力</strong>。Profiles 是为这个存在的。</p>

  <h3 id="ap-3">AP-3：AI 写完 spec 不 lint</h3>
  <p>AI 写的 spec <strong>几乎一定有瑕疵</strong>——字段遗漏、Scenario 抽象、Acceptance 不可测。4 段 Lint 是必经步骤，不是可选项。</p>

  <h3 id="ap-4">AP-4：archive 后不更新 spec</h3>
  <p>archive 后实现细节可能漂移 spec。<strong>定期 reconcile</strong>（每月 / 每季度）——把实现拉回 spec 权威。</p>

  <h3 id="ap-5">AP-5：把 review 决议只写在 IM 里</h3>
  <p>评审决议必须落在 spec 旁边的 <code>review-X-resolution.md</code>，否则半年后没人知道为什么这么改。决议和 spec 一样是<strong>source of truth</strong>。</p>

  <h2 id="metrics">调优成效信号</h2>
  <p>三个月后回来看这些数字（以下为<strong>待团队校准的参考阈值</strong>，不是行业事实）：</p>

  <ul>
    <li><strong>spec 覆盖率</strong>：生产代码里有多少 % 对应 spec 编号。参考目标 ≥ 80%。</li>
    <li><strong>open_questions 解决率</strong>：30 天内的 open_questions 解决了多少 %。参考目标 ≥ 90%。</li>
    <li><strong>archive 后实现漂移率</strong>：archive 后多少 % 的 PR 引用 spec 编号。参考目标 ≥ 70%。</li>
    <li><strong>团队主动率</strong>：团队自己主动写 spec 的比例（非被要求）。参考目标 ≥ 60%。</li>
  </ul>

  <p>如果<strong>覆盖率不涨</strong>，说明 spec 流程在落地阻力上失败了——回去看 AP 反模式。</p>

  <h2 id="related">相关阅读</h2>
  <ul>
    <li><a href="/handbook/why-spec-driven-dev">为什么需要 Spec-Driven-Dev</a> — 我们的工具定位</li>
    <li><a href="/handbook/spec-format">Spec 字段规范</a> — 13 字段格式（中文工业流程）</li>
    <li><a href="/handbook/workflow">6 步工作流</a> — 模糊需求 → Frozen Spec</li>
    <li><a href="/handbook/lint-protocol">AI Lint 协议</a> — 4 段对抗性 AI 评审</li>
    <li><a href="/research/agent-ecosystem">Coding Agent 生态全景</a> — 上下游工具图谱</li>
    <li><a href="/research/landscape">行业全景图</a> — 2026 年 SDD 工具整体格局</li>
  </ul>
</MainLayout>