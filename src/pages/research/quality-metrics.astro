---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="AI 代码质量度量" description="AI 生成代码的质量如何度量？从 SWE-bench、churn rate、bug 率和开发者信任度建立团队指标">
  <Sidebar currentPath="/research/quality-metrics" slot="sidebar" />
  <h1>AI 代码质量度量</h1>
  <div class="meta-line"><span class="tag">调研数据</span> 2026 年 7 月 · 部分阈值需按团队历史数据校准</div>

  <h2 id="problem">问题：怎么判断 AI 代码「好不好」？</h2>
  <p>传统指标（测试覆盖率、lint 通过率）在 AI 时代严重不够用。因为 AI 可以「过测试」但行为错，AI 代码可以「看起来漂亮」但逻辑有漏洞。</p>

  <h2 id="benchmarks">基准测试：从 HumanEval 到 SWE-bench</h2>
  <table>
    <thead><tr><th>基准</th><th>测试内容</th><th>难度</th><th>顶级模型得分 (2026)</th></tr></thead>
    <tbody>
      <tr><td>HumanEval</td><td>独立函数生成</td><td>低</td><td>90%+（已饱和）</td></tr>
      <tr><td>MBPP</td><td>Python 基础编程</td><td>低</td><td>85%+（近饱和）</td></tr>
      <tr><td>SWE-bench Verified</td><td>真实 GitHub Issue 修复</td><td><strong>高</strong></td><td>头部模型约 40-50%（随日期和口径变化）</td></tr>
      <tr><td>SWE-bench Multilingual</td><td>多语言 Issue 修复</td><td>很高</td><td>~30%</td></tr>
      <tr><td>AgentBench</td><td>多步骤 Agent 任务</td><td>很高</td><td>~25-35%</td></tr>
    </tbody>
  </table>
  <div class="admonition warning">
    <div class="admonition-title">关键认知</div>
    <p>HumanEval 已经不能用来评估 AI 代码能力。SWE-bench 更能反映真实工程能力，但最高分也只在 40-50%——<strong>AI 在真实工程任务上还有很长的路</strong>。</p>
  </div>

  <h2 id="real-metrics">生产环境中的真实度量</h2>
  <h3>1. AI vs Human Turnover Ratio（代码翻修率）</h3>
  <p>建议把 AI 代码提交后的翻修率作为团队内部预警指标，而不是直接套用行业固定倍数：</p>
  <blockquote>
    <p>如果 AI 生成代码的 churn rate（提交后 3 周内被修改的比例）持续显著高于人工代码基准，说明 AI 代码占比、Spec 质量或 review 强度需要重新校准。</p>
  </blockquote>
  <p>可先用 <code>1.3x</code> 作为保守观察线、<code>1.5x</code> 作为预警线，再按团队历史数据调整。</p>

  <h3>2. 开发者信任度下降趋势</h3>
  <p>公开开发者调查反复显示：AI 工具普及率上升，并不自动等于开发者对输出准确性更信任。</p>
  <ul>
    <li>跟踪开发者对 AI 输出准确性的信任趋势，而不是只看工具安装率</li>
    <li>跟踪调试 AI 代码是否超出预期，以及问题在 30/60/90 天后的复发率</li>
    <li>把“信任下降”当成流程信号：可能是 review、测试、Spec 或上下文供给没有跟上</li>
  </ul>

  <h3>3. AI 代码的 Bug 引入率</h3>
  <p>GitClear、Qodo 等报告都提示：AI 对短期开发速度有帮助，但长期质量要看 review 和测试是否跟上。具体数值应以团队自己的缺陷归因数据为准。</p>
  <ul>
    <li>短期看完成速度和 PR cycle time</li>
    <li>中期看 bug 密度、重复代码、回滚率和返工率</li>
    <li>长期看 AI 代码是否更难修改、测试是否更弱、事故归因是否集中</li>
  </ul>

  <h2 id="metric-framework">建议的度量框架</h2>
  <table>
    <thead><tr><th>指标</th><th>测量什么</th><th>预警阈值</th></tr></thead>
    <tbody>
      <tr><td><strong>Churn Rate</strong></td><td>AI 代码的翻修频率</td><td>&gt; 1.5x 人工基准可先作为预警线</td></tr>
      <tr><td><strong>Review Pass Rate</strong></td><td>AI PR 首次通过率</td><td>&lt; 60% 可能 prompt 有问题</td></tr>
      <tr><td><strong>Bug Attribution</strong></td><td>AI 代码 bug 占比 vs 代码量占比</td><td>AI bug% &gt; AI code% — 需要加严 review</td></tr>
      <tr><td><strong>Spec Traceability</strong></td><td>多少代码变更能追溯到 Spec</td><td>&lt; 80% — spec 失去权威</td></tr>
      <tr><td><strong>Time-to-Resolve</strong></td><td>AI 代码 vs 人工代码的 bug 修复时间</td><td>AI &gt; 1.3x 人工 — 可维护性差</td></tr>
    </tbody>
  </table>

  <h2 id="anti-patterns">常见度量反模式</h2>
  <ol>
    <li><strong>只看测试覆盖率。</strong> AI 可以生成「覆盖率高但断言弱」的测试。要检查断言质量。</li>
    <li><strong>只看 lint 通过。</strong> AI 代码 lint 通过率极高（因为 AI 擅长语法），但逻辑错误 lint 检测不到。</li>
    <li><strong>忽略代码简化度。</strong> AI 倾向于过度工程化。度量复杂度（cyclomatic complexity）和抽象层数。</li>
    <li><strong>不追踪长期指标。</strong> 只看当下，不看 3 个月后的维护成本。</li>
  </ol>

  <h2 id="takeaway">关键结论</h2>
  <blockquote>
    <p>AI 让代码写得快，但代码质量是<strong>流程的函数</strong>，不是模型的函数。好流程 + 弱模型 > 坏流程 + 强模型。</p>
  </blockquote>
</MainLayout>
