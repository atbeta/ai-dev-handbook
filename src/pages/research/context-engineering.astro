---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="上下文工程（行业视角）" description="AI 辅助开发中的上下文工程：如何给 AI 提供高效上下文 - AGENTS.md、Repo Map、MCP 工具接入">
  <Sidebar currentPath="/research/context-engineering" slot="sidebar" />
  <h1>上下文工程（行业视角）</h1>
  <div class="meta-line"><span class="tag">核心技能</span> 2026 年 7 月</div>

  <h2 id="what">什么是上下文工程？</h2>
  <blockquote>
    <p>上下文工程是系统性地设计、管理和优化提供给 AI 的上下文信息，以最大化其编程产出质量的实践。</p>
  </blockquote>
  <p>2026 年，上下文工程成为 AI 开发讨论中的高频主题。许多实践观察都指向同一个结论：比起继续打磨单条 prompt，系统性地管理项目结构、约定、任务输入和工具上下文，往往更能稳定提升 AI 产出质量。</p>
  <p>本页偏行业视角；具体工具落地见 <a href="/handbook/context-engineering">上下文工程（工具实践）</a>。</p>

  <h2 id="pyramid">上下文金字塔</h2>
  <table>
    <thead><tr><th>层级</th><th>内容</th><th>更新频率</th><th>影响面</th></tr></thead>
    <tbody>
      <tr><td><strong>L0: 全局规则</strong></td><td>AGENTS.md、CLAUDE.md、.cursorrules</td><td>每月</td><td>所有任务</td></tr>
      <tr><td><strong>L1: 项目结构</strong></td><td>目录树、模块关系图、技术栈说明</td><td>每次架构变更</td><td>跨模块任务</td></tr>
      <tr><td><strong>L2: 编码规范</strong></td><td>命名约定、错误处理模式、测试策略、Glossary</td><td>每季度</td><td>所有代码生成</td></tr>
      <tr><td><strong>L3: 任务上下文</strong></td><td>Spec 文档、相关代码文件、依赖 schema</td><td>每次任务</td><td>单任务</td></tr>
      <tr><td><strong>L4: 运行时上下文</strong></td><td>错误日志、测试输出、lint 结果</td><td>实时</td><td>调试、修复</td></tr>
    </tbody>
  </table>

  <h2 id="agents-md">AGENTS.md：项目的「AI 入职手册」</h2>
  <p>AGENTS.md 是上下文工程的核心文件之一。它通常位于项目根目录，用来给 Agent 型工具提供项目结构、约定和约束；不同工具的读取规则并不完全相同。</p>

  <h3>一份好的 AGENTS.md 应该包含</h3>
  <pre><code># AGENTS.md

## Project Overview
- 项目名称、一句话描述
- 技术栈（语言、框架、数据库）
- 部署方式

## Architecture
- 目录结构说明
- 模块划分与职责
- 数据流方向

## Conventions
- 命名规范（文件、函数、变量、数据库字段）
- 错误处理模式
- 日志格式
- 测试策略

## Constraints
- 不能使用的库/模式
- 性能要求（P99 延迟、QPS 上限）
- 安全要求（禁止的 API、数据脱敏规则）

## Glossary
- 业务术语 → 代码概念映射
- 易混淆概念区分</code></pre>

  <div class="admonition tip">
    <div class="admonition-title">AGENTS.md 编写原则</div>
    <p>不要写「应该怎么做」，写「这里就是这么做的」。AGENTS.md 是<strong>既成事实</strong>的文档，不是「最佳实践」的建议。AI 会严格遵循它读到的约定。</p>
  </div>

  <h2 id="repo-map">Repo Map：大项目的结构化导航</h2>
  <p>对于 5 万行以上的项目，AI 无法一次性理解全部代码。Repo Map 是一种轻量级的项目地图：</p>
  <ul>
    <li><strong>文件树 + 摘要</strong>：每个关键文件一行描述其职责</li>
    <li><strong>模块边界</strong>：标注模块间依赖方向和接口约定</li>
    <li><strong>入口点</strong>：API 端点、CLI 命令、事件处理器的位置</li>
  </ul>

  <h2 id="mcp-tools">MCP 工具：给 AI 装上「手」</h2>
  <p>MCP 让 AI 不只是读代码，还能操作外部系统。关键的 MCP 工具接入：</p>
  <table>
    <thead><tr><th>MCP Server</th><th>能力</th><th>典型场景</th></tr></thead>
    <tbody>
      <tr><td>数据库 MCP</td><td>读写数据、执行 SQL、查看 schema</td><td>数据迁移、查询验证</td></tr>
      <tr><td>Git MCP</td><td>提交、分支管理、PR 操作</td><td>自动化 git 工作流</td></tr>
      <tr><td>浏览器 MCP</td><td>截图、DOM 操作、控制台</td><td>UI 调试、E2E 测试</td></tr>
      <tr><td>API 文档 MCP</td><td>查询第三方 API schema</td><td>集成开发（如 Shopify AI Toolkit）</td></tr>
    </tbody>
  </table>

  <h2 id="principles">上下文工程的 5 条原则</h2>
  <ol>
    <li><strong>最小必要原则。</strong> 不是越多越好。上下文过多会稀释注意力，反而降低准确性。</li>
    <li><strong>分层原则。</strong> 全局约定放 AGENTS.md，任务特定信息放 Spec 文档。</li>
    <li><strong>事实性原则。</strong> 写「是什么」不写「应该是什么」。AI 把前者当约束，把后者当建议。</li>
    <li><strong>版本同步原则。</strong> 代码变了，AGENTS.md 和 Repo Map 也要更新。过期上下文比没上下文更危险。</li>
    <li><strong>可验证原则。</strong> L0-L2 层的上下文更新应该触发 CI 检查（如 lint 规则一致性）。</li>
  </ol>

  <h2 id="anti-patterns">上下文工程的反模式</h2>
  <ul>
    <li><strong>AGENTS.md 写成「最佳实践」文集。</strong> AI 需要的是「这个项目怎么做」，不是「业界怎么做」。</li>
    <li><strong>上下文堆积。</strong> 把所有东西塞进 prompt——AI 会被淹没，关注点分散。</li>
    <li><strong>只建不维护。</strong> AGENTS.md 写了就放那，代码演进后不更新。</li>
    <li><strong>把 AGENTS.md 当「写给人的 README」。</strong> 给人看的和给 AI 看的内容结构不同。AI 需要结构化的约束，人需要叙述性的理解。</li>
  </ul>
</MainLayout>
