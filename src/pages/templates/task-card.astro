---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="任务卡模板" description="AI 协作任务卡模板——每个任务卡的 6 个必填项">
  <Sidebar currentPath="/templates/task-card" slot="sidebar" />
  <h1>任务卡模板</h1>
  <div class="meta-line"><span class="tag">模板</span> 直接复制，填内容即可</div>

  <h2 id="template">模板</h2>
<pre><code># Task: [TASK-ID] [任务简述]

> 状态: [Todo / In Progress / In Review / Done]
> 创建人: [姓名]
> 创建日期: [YYYY-MM-DD]
> 预估 token: [1000-5000]
> 目标时长: [< 30 分钟]

---

## 1. 范围（不可逾越的边界）

### 做什么
- [具体的、可验收的产出]
- [可验收的产出 2]

### 不做什么（显式排除）
- [明确不在此任务范围内的]
- [避免 scope creep]

### 依赖
- [依赖的已完成任务 / 已存在的文件 / 接口]

---

## 2. 上下文文件（AI 需要读的文件）

| 文件路径 | 为什么需要 | 优先级 |
|---------|-----------|--------|
| src/models/User.ts | 领域模型定义 | 必须 |
| src/services/AuthService.ts | 认证逻辑参考 | 必须 |
| specs/user-crud/spec.md | 功能 Spec | 推荐 |

> 总 token 量估算：约 [X]K token（目标 < 15K）

---

## 3. 验收测试（Before = Done）

- [ ] 验收点 1: [具体描述，可自动化判断]
- [ ] 验收点 2: [具体描述]
- [ ] 验收点 3: [边界条件]

### 测试用例（如适用）
```typescript
// Test: [用例名称]
// Given: [前置条件]
// When: [触发动作]
// Then: [期望结果]
```

---

## 4. AI 提示片段

```
You are implementing: [TASK-ID] [任务简述]

CRITICAL CONTEXT:
[来自 Constitution 的相关规则引用]
[来自 Spec 的关键约束]

WHAT TO DO:
1. [步骤 1]
2. [步骤 2]
3. [步骤 3]

WHAT NOT TO DO:
- [明确的禁止事项]

CONTRACTS:
- Input: [输入契约引用]
- Output: [输出契约引用]

VERIFICATION:
After implementation, verify:
- [验收测试 1]
- [验收测试 2]
```

---

## 5. 任务特征评估

| 特征 | 是/否 | 说明 |
|------|-------|------|
| 跨系统？ | [ ] | |
| 长程（> 半天）？ | [ ] | |
| 复杂业务逻辑？ | [ ] | |
| 接口/数据差异？ | [ ] | |

> 死亡特征数: [0-4]
> 建议分工模式: [AI 全做 / 契约分工 / 人做]

---

## 6. 完成记录

| 项目 | 记录 |
|------|------|
| 实际耗时 | [X 分钟] |
| 实际 token | [X]K |
| 重做次数 | [X] 次 |
| 熔断触发？ | [是/否] |
| Gate 通过？ | [是/否] |
| 备注 | [学到的东西 / 踩的坑] |
</code></pre>
</MainLayout>
