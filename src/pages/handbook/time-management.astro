---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="时间管理" description="AI 编程下的时间管理：3 任务流、长短任务派单、Files ARE the State 思路接续">
  <Sidebar currentPath="/handbook/time-management" slot="sidebar" />
  <h1>时间管理</h1>
  <div class="meta-line"><span class="tag">实践指南</span> <span class="tag">节奏</span> <span class="tag">异步</span></div>

  <blockquote>
    <p>等 AI 时最亏的就是盯着 token 倒计时，那叫精神内耗。把"等"换成"切流"，时间就回来了。</p>
  </blockquote>

  <h2 id="three-streams">3 任务流（不要"等 AI"）</h2>
  <p>把一天切成三条流。"等 AI"不是第四种状态——是切到流 3 的触发器。</p>

  <table>
    <thead><tr><th>流</th><th>干什么</th><th>响应要求</th><th>举例</th></tr></thead>
    <tbody>
      <tr><td><strong>流 1: 同步对话</strong></td><td>复杂设计、方案讨论、SPEC 评审</td><td>5 分钟内响应</td><td>跟 AI 争论方案、评审 diff</td></tr>
      <tr><td><strong>流 2: 异步长任务</strong></td><td>派任务 → 走开 → 通知 → 不盯</td><td>不响应</td><td>派 migration、夜间任务</td></tr>
      <tr><td><strong>流 3: 体力活</strong></td><td>写 PR、回邮件、补文档</td><td>可随时打断</td><td>PR description、Slack 回消息</td></tr>
    </tbody>
  </table>

  <p><strong>关键纪律</strong>：等 AI 那一分钟不是"等"，是切到流 3（写文档、回 PR 评论）。30 秒一个来回。</p>

  <h2 id="long-vs-short">长任务 vs 短任务派单原则</h2>

  <table>
    <thead><tr><th>任务特征</th><th>派单方式</th><th>人怎么接续</th></tr></thead>
    <tbody>
      <tr><td>30 分钟以内、能完整描述</td><td>同步流，自己盯</td><td>不需要接续</td></tr>
      <tr><td>30 分钟至 2 小时，验收明确</td><td>异步派加通知</td><td>回来时看任务页加 diff</td></tr>
      <tr><td>2 小时以上，跨模块</td><td><strong>必须先拆</strong>成多个 2 小时以内子任务</td><td>每个子任务有产物，接续看产物</td></tr>
      <tr><td>跨第三方 / 不确定能不能完成</td><td>同步流，人在场随时止损</td><td>不允许异步派</td></tr>
    </tbody>
  </table>

  <div class="admonition warning">
    <div class="admonition-title">不许异步派的场景</div>
    <p>任何"我不确定 AI 能不能完成"的任务，必须同步流、必须人在场。跨第三方 API 调用、不熟悉的遗留模块、首次接触的新技术栈——都属于这一类。</p>
  </div>

  <h2 id="files-are-state">Files ARE the State（思路接续的核心）</h2>
  <p>离开电脑前、第二天回来时，<strong>不要靠记忆接续</strong>。所有上下文必须落地到文件。</p>

  <h3>TASK_STATE.md 模板</h3>
  <pre><code>{`# 任务：迁移 billing 模块

## 当前阶段
Plan 完成 / Executor 进行中

## 已完成
- 读懂旧 billing_service.go
- 写新 billing_v2/billing.go 骨架

## 阻塞
- 第三方支付 SDK v3 没在 SPEC 里，需要人确认

## 下一步
- 实现退款逻辑（如果第三方 SDK 确认可用）

## 不在范围
- 不动发票模板（那是另一个 PR）`}</code></pre>
  

  <h3>三个使用时刻</h3>
  <ol>
    <li><strong>AI 接手时</strong>：第一件事读 TASK_STATE.md，明确自己处在哪一步</li>
    <li><strong>离开电脑前最后 1 分钟</strong>：把刚才想到的、写下来的、还没成型的判断全压到这个文件</li>
    <li><strong>第二天回来时</strong>：不要靠记忆，直接读 TASK_STATE.md + git log + 任务页</li>
  </ol>

  <h2 id="hand-off">交接的标准动作（5 步）</h2>
  <ol>
    <li><strong>打开任务页</strong>：看任务状态、最后活跃时间、错误日志（如果有）</li>
    <li><strong>读 TASK_STATE.md</strong>：了解当前进度和阻塞</li>
    <li><strong>看 diff</strong>：跑 git diff main..HEAD --stat，把握整体改动面</li>
    <li><strong>对照 SPEC.md</strong>：逐条核对验收标准</li>
    <li><strong>决策</strong>：通过 / 反馈重做 / 人接手 / 废弃</li>
  </ol>

  <p>这 5 步控制在 10 分钟内完成——晨会第一件事搞定，剩余时间留给流 1 同步对话。</p>

  <h2 id="anti-patterns">时间管理的反模式</h2>

  <h3>反模式 1：盯 token 倒计时</h3>
  <p>盯着 AI 输出流发呆，等它说完——这叫精神内耗。</p>
  <p><strong>纠正</strong>：派完任务立刻切流 3，30 秒后回来扫一眼进度条，没崩就走。</p>

  <h3>反模式 2：单任务串行到天黑</h3>
  <p>上午派 1 个任务、等到中午、下午派 1 个任务、等到下班——1 天只干 2 件事。</p>
  <p><strong>纠正</strong>：早 8 点一次性派 3 至 5 个独立任务（worktree 隔离），中间穿插流 1 和流 3。</p>

  <h3>反模式 3：长任务不拆</h3>
  <p>"把这个模块从 A 迁移到 B"——一句话派给 AI，结果 4 小时没完，没法中断，没法接续。</p>
  <p><strong>纠正</strong>：强制拆成 2 小时以内的子任务，每个有独立验收标准。</p>

  <h3>反模式 4：离开时不写 TASK_STATE</h3>
  <p>"反正我明天记得"——第二天回来一脸懵。</p>
  <p><strong>纠正</strong>：离开前最后 1 分钟强制动作——打开 TASK_STATE.md，写一行"当前状态"。</p>

  <div class="admonition tip">
    <div class="admonition-title">一句话总结</div>
    <p>Files ARE the State。记忆会骗你，文件不会。把所有上下文压到磁盘上，AI 接管时省 80% 的"重新对齐"时间。</p>
  </div>

  <p style="margin-top:24px"><a href="/handbook/async-nightly">下一章：夜间异步执行 →</a></p>
</MainLayout>