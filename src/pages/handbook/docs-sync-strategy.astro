---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="文档同步策略" description="AI 时代的文档与实现一致性：分层策略、自验证处理流程、同 PR 同步纪律">
  <Sidebar currentPath="/handbook/docs-sync-strategy" slot="sidebar" />
  <h1>文档同步策略</h1>
  <div class="meta-line"><span class="tag">实践指南</span> <span class="tag">文档</span> <span class="tag">纪律</span></div>

  <blockquote>
    <p>绝对不要"先改文档再改代码"或反过来——同 PR、同次评审、同次合并。</p>
  </blockquote>

  <h2 id="two-schools">业界的两个对立流派</h2>

  <h3>流派 A：文档即代码的"自维护层"</h3>
  <p>代表：agent-guard / Tessl / dev.to 实践</p>
  <ul>
    <li>用脚本从源码<strong>反向生成</strong> inventory（API 路由、Prisma models、env vars）</li>
    <li>这些<strong>自动生成</strong>的文档是真相，AI 必须基于它写代码</li>
    <li>人类写的文档是参考，可以漂移但要被 lint 抓出来</li>
  </ul>

  <h3>流派 B：PR 流程强制同步</h3>
  <p>代表：ClickUp / Microsoft 推荐</p>
  <ul>
    <li>PR checklist 必勾"文档已更新"</li>
    <li>每次合并时检查文档 diff</li>
  </ul>

  <h2 id="three-layers">建议：分层 + 阶段</h2>
  <p>两个流派都对，但适用不同类型。我的建议是<strong>三层文档</strong>：</p>

  <table>
    <thead><tr><th>层级</th><th>类型</th><th>谁维护</th><th>漂移容忍度</th><th>更新时机</th></tr></thead>
    <tbody>
      <tr>
        <td><strong>L1: 自动生成</strong></td>
        <td>API 清单、数据模型、env 变量</td>
        <td>脚本</td>
        <td>不允许漂移</td>
        <td>每次代码合并自动重生成</td>
      </tr>
      <tr>
        <td><strong>L2: 任务级 SPEC</strong></td>
        <td>每个 PR 的 SPEC.md、决策记录</td>
        <td>人 + AI</td>
        <td>漂移可接受</td>
        <td>同 PR 修改</td>
      </tr>
      <tr>
        <td><strong>L3: 架构 / 愿景</strong></td>
        <td>ADR / RFC / README</td>
        <td>人</td>
        <td>漂移可接受</td>
        <td>季度 / 月度维护</td>
      </tr>
    </tbody>
  </table>

  <h2 id="l1-auto">L1: 自动生成（永远不漂移）</h2>

  <h3>典型工具</h3>
  <ul>
    <li><strong>TypeScript</strong>：typedoc / api-extractor</li>
    <li><strong>Python</strong>：sphinx-autodoc / mkdocstrings</li>
    <li><strong>Go</strong>：godoc / swag</li>
    <li><strong>通用</strong>：agent-guard（零依赖，从源码反向提取）</li>
  </ul>

  <h3>CI 强制</h3>
  <pre><code>{`# .github/workflows/docs-lint.yml
name: Docs Drift Check
on: [pull_request]
jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: npm run docs:generate
      - run: |
          if [[ -n $(git diff --name-only docs/auto-generated/) ]]; then
            echo "Docs out of date. Run docs:generate and commit."
            exit 1
          fi`}</code></pre>
  

  <p><strong>规则</strong>：CI 检查 L1 文档是否与代码同步，不同步直接挂 PR。</p>

  <h2 id="l2-spec">L2: 任务级 SPEC（漂移可接受）</h2>

  <p>每个 PR 一份 SPEC.md，归档到 docs/archive 目录。漂移了不致命——下次新 PR 重新对齐。</p>

  <h3>绝对纪律</h3>
  <div class="admonition warning">
    <div class="admonition-title">同 PR 同步</div>
    <p>改代码必须改 SPEC（或反向），不允许单独 PR。SPEC 是任务的契约，代码是实现，两者必须绑在一起评审、合并。</p>
  </div>

  <h2 id="l3-arch">L3: 架构 / 愿景（季度维护）</h2>

  <p>ADR (Architecture Decision Record) / RFC 类文档。<strong>平时不维护</strong>，只在重大决策时写。</p>

  <h3>季度维护节奏</h3>
  <ul>
    <li>每季度一次"架构文档体检"（2 小时）</li>
    <li>发现重大偏差：写 ADR 记录变更</li>
    <li>小偏差：累积到下季度处理</li>
  </ul>

  <h2 id="drift-handling">自验证发现问题时怎么办</h2>

  <table>
    <thead><tr><th>文档类型</th><th>处理方式</th></tr></thead>
    <tbody>
      <tr>
        <td>L1（自动生成）不一致</td>
        <td>CI 直接挂。<strong>先修代码</strong>，让生成器重跑 → 自动更新文档</td>
      </tr>
      <tr>
        <td>L2（任务级 SPEC）</td>
        <td>PR 里<strong>一起改 SPEC + 代码</strong>，不允许单独 PR</td>
      </tr>
      <tr>
        <td>L3（架构文档）</td>
        <td>攒到周末或月底统一维护，写 ADR 记录变更</td>
      </tr>
    </tbody>
  </table>

  <div class="admonition danger">
    <div class="admonition-title">绝对禁止</div>
    <p>不要"先改文档再改代码"或反过来——这是 GitHub Spec Kit 的核心纪律。文档和代码是同一决策的两个表达，必须同时更新、同时评审、同时合并。</p>
  </div>

  <h2 id="agent-sync">AI 时代的特殊纪律</h2>

  <h3>1. 让 AI 在改代码时同步改 SPEC</h3>
  <p>在 SPEC.md 头部加一行：</p>
  

  <h3>2. pre-commit hook 检查</h3>
  <pre><code>{`# .husky/pre-commit
#!/usr/bin/env sh
# 检查本次 commit 修改的代码文件是否伴随文档更新
if git diff --cached --name-only | grep -E '\.(ts|py|go)$'; then
  if ! git diff --cached --name-only | grep -E '\.md$'; then
    echo "代码改动未伴随文档更新，确认是否需要：(y/N)"
    read -r response
    [[ "$response" =~ ^[Yy]$ ]] || exit 1
  fi
fi`}</code></pre>
  

  <h3>3. AI 写完代码后强制 lint</h3>
  <p>在 SPEC.md 的验收标准里加一条：</p>
  

  <h2 id="ai-coding-failures">AI 编码特有的文档漂移</h2>

  <h3>失败模式 1：AI 幻觉 API</h3>
  <p>AI 生成代码调用了不存在的 API，而 L1 文档是真实的——CI 会抓到。</p>
  <p><strong>纠正</strong>：把 L1 自动生成文档作为 AI 的强制上下文（写入 system prompt）。</p>

  <h3>失败模式 2：AI 改代码不改 SPEC</h3>
  <p>AI 在重构时跳过了 SPEC 更新环节。</p>
  <p><strong>纠正</strong>：在 SPEC.md 验收标准里加"SPEC 同步"硬约束。</p>

  <h3>失败模式 3：AI 引用过期 ADR</h3>
  <p>AI 读了 2 年前的 ADR，照着过时设计写代码。</p>
  <p><strong>纠正</strong>：每个 ADR 头部加 superseded by 字段。</p>

  <div class="admonition tip">
    <div class="admonition-title">一句话总结</div>
    <p>L1 自动生成强制不漂移，L2 任务 SPEC 同 PR 同步，L3 架构文档季度维护。绝对不要"先文档后代码"或反过来——同 PR、同次评审、同次合并。</p>
  </div>

  <p style="margin-top:24px"><a href="/research/infrastructure-impact">下一章：公司 AI 基础设施影响 →</a></p>
</MainLayout>