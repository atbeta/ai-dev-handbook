---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="不熟项目 + AI 实战" description="人类对遗留项目不熟悉时，如何用三阶段渐进信任驾驭 AI 完成迁移和重构">
  <Sidebar currentPath="/handbook/unfamiliar-codebase" slot="sidebar" />
  <h1>不熟项目 + AI 实战</h1>
  <div class="meta-line"><span class="tag">实践指南</span> <span class="tag">遗留项目</span> <span class="tag">迁移</span></div>

  <blockquote>
    <p>人必须先建立心智地图，AI 才能当工具。否则 AI 是放大器，放大的是你的无知。</p>
  </blockquote>

  <h2 id="core-tension">核心矛盾</h2>
  <p>公司试点里最致命也最高频的场景：人没有业务心智地图，AI 上下文有限还自信，业务深度耦合且有第三方依赖——三者叠加必然是返工和误导。</p>

  <table>
    <thead><tr><th>角色</th><th>弱点</th></th></tr></thead>
    <tbody>
      <tr><td>人</td><td>不熟项目、无法判断 AI 对错</td></tr>
      <tr><td>AI</td><td>上下文有限、幻觉、自信心比准确率高</td></tr>
      <tr><td>业务</td><td>深度耦合、第三方依赖、历史包袱</td></tr>
    </tbody>
  </table>

  <h2 id="three-stages">三阶段渐进信任（不许跳级）</h2>

  <table>
    <thead><tr><th>阶段</th><th>你做什么</th><th>AI 能做什么</th><th>任务规模</th><th>期限</th></tr></thead>
    <tbody>
      <tr><td><strong>1. 考古</strong></td><td>自己读入口文件 + 画心智地图</td><td>只读、回答问题、列清单</td><td>不动代码</td><td>2 至 4 小时</td></tr>
      <tr><td><strong>2. 侦察</strong></td><td>跟着 AI 巡检</td><td>只读、找 bug、找边界</td><td>不动代码</td><td>1 至 2 天</td></tr>
      <tr><td><strong>3. 协作</strong></td><td>你写 SPEC、AI 执行、你审</td><td>写代码 + 测试 + 文档</td><td>小 至 中 至 大</td><td>持续</td></tr>
    </tbody>
  </table>

  <div class="admonition warning">
    <div class="admonition-title">硬纪律</div>
    <p>不许跳过阶段 1。第 1 天就让 AI 改 migration 的团队，第 3 天都会发现改了 5 个不该动的模块。</p>
  </div>

  <h2 id="stage-1">阶段 1：考古（你亲自动手）</h2>
  <p>这一阶段 AI 只能跑命令、整理输出，<strong>不许 AI 总结架构</strong>——它会基于猜测写得很漂亮，误导你。</p>

  <h3>你的 4 件事</h3>
  <ol>
    <li><strong>入口三件套</strong>：主入口文件、配置文件、README/ARCHITECTURE</li>
    <li><strong>目录结构</strong>：跑 tree 命令看两层目录</li>
    <li><strong>画心智地图</strong>：谁调用谁、数据流向、哪些是边界模块（接第三方/外部 API）</li>
    <li><strong>记下 5 个最不懂的点</strong>，下一步让 AI 回答</li>
  </ol>

  <h2 id="stage-2">阶段 2：侦察（4 轮只读 prompt）</h2>

  <h3>第 1 轮：地图</h3>
  <pre><code>{`你是一个代码考古学家，不许修改任何代码。
任务：基于事实（附文件路径加行号），输出：
1. 这个项目的入口点和它们的关系
2. 三到五条最关键的数据流
3. 三个"复杂度炸弹"位置（高耦合/副作用/第三方集中）
不要给建议，只给事实。`}</code></pre>

  <h3>第 2 轮：陷阱清单</h3>
  <pre><code>{`不许修改代码。
基于上面的事实，列出五个最容易踩坑的"边界"：
- 看起来丑但不能动的地方（历史包袱）
- 看起来漂亮但有副作用的地方
- 跨第三方调用点
每个标出对应的文件加行号。`}</code></pre>

  <h3>第 3 轮：自相矛盾点</h3>
  <pre><code>{`不许修改代码。
找出三处"看起来对、其实错"的代码：
- 文档与实现不符
- 注释与代码不符
- 测试与实现不符
每处给出证据（git blame 加当前代码 diff）。`}</code></pre>

  <h3>第 4 轮：灵魂</h3>
  <pre><code>{`不许修改代码。
我要接手这个项目，请用 5 分钟带我过：
- 这个项目的"灵魂"是什么（为什么存在，不是技术栈）
- 如果你来给新人推荐 3 个文件先读，会是哪 3 个
- 5 年后回看，最可能被重写的部分是哪里
不要谦虚，不要套话，给判断。`}</code></pre>

  <p>这 4 轮之后你才真正"懂"这个项目，然后才能写 SPEC。</p>

  <h2 id="stage-3">阶段 3：协作纪律</h2>

  <h3>3 不许</h3>
  <ul>
    <li>不许让 AI 一上来生成架构图（基于猜测）</li>
    <li>不许让 AI 直接做跨模块 migration</li>
    <li>不许用 AI 写测试来"证明"代码对（自证等于无意义）</li>
  </ul>

  <h3>5 条纪律</h3>
  <ol>
    <li><strong>每条验收标准都要有"探索性证据"</strong>：不是"接口返回 X"，而是"接口在 commit abc123 之后返回 X，依据是 yyy.md"</li>
    <li><strong>跨模块调用前必须人肉跑一次</strong>：AI 让你改 A 函数 → 你先自己把 A 的调用链走一遍 → 再让 AI 改</li>
    <li><strong>SPEC 加一段"我对这个项目的已知误解"</strong>：你读代码时产生的疑问，全部写下来，让 AI 帮你证伪/证实</li>
    <li><strong>任务规模渐进</strong>：1 至 3 个任务 20 行以内，4 至 10 个 100 行以内，10 以上才能到 200 行</li>
    <li><strong>每日学习闭环</strong>：下班前 10 分钟写每日 learnings 笔记</li>
  </ol>

  <h2 id="killer-prompt">杀手锏：让 AI 当学生，你当老师</h2>
  <p>很多团队让 AI 当老师（"教我懂这个项目"），方向反了。正确的是让 AI 答你的题：</p>
  <pre><code>{`我刚接手这个项目，请你当学生。
我要测验你三件事（不许猜，要给文件加行号证据）：
1. 这个项目最不为人知的"隐含契约"是什么？
2. 哪个文件如果删了，会出大问题但不报错？
3. 哪个第三方依赖的版本钉死了，原因是什么？
答错就回滚你的回答重做。`}</code></pre>

  

  <p><strong>为什么有效</strong>：AI 被迫"答对"，就得真的读代码、找证据。这是它最高质量的输出场景。</p>

  <h2 id="role-split">角色定位（最后一遍强调）</h2>
  <table>
    <thead><tr><th>角色</th><th>干什么</th><th>不能干什么</th></tr></thead>
    <tbody>
      <tr><td><strong>人</strong></td><td>考古队长加决策者</td><td>不能甩给 AI</td></tr>
      <tr><td><strong>AI</strong></td><td>考古队员加资料整理员</td><td>不能做决策，尤其不能决定"做不到就停下来"以外的事</td></tr>
    </tbody>
  </table>

  <div class="admonition tip">
    <div class="admonition-title">一句话总结</div>
    <p>不熟项目时，AI 是你的侦察兵不是将军。AI 跑得快但不知道往哪跑，你跑得慢但知道目标在哪——别把将军的活交给侦察兵。</p>
  </div>

  <p style="margin-top:24px"><a href="/handbook/parallel-sessions">下一章：多 AI 会话并行 →</a></p>
</MainLayout>