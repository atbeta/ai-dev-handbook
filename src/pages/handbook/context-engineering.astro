---
import MainLayout from '../../layouts/MainLayout.astro';
import Sidebar from '../../components/Sidebar.astro';
---

<MainLayout title="上下文工程" description="给 AI 喂什么上下文：AGENTS.md、CodeGraph、Repo Map、MCP 的工具实践">
  <Sidebar currentPath="/handbook/context-engineering" slot="sidebar" />
  <h1>上下文工程</h1>
  <div class="meta-line"><span class="tag">工具栈</span> Context Engineering · Layer 1</div>

  <blockquote>
    <p>AI 是放大器，上下文是它的乘数。给 AI <code>模糊需求 + 整库代码</code>，产出是 <code>模糊 × 噪声</code>。给 AI <code>清晰 Spec + 精准切片</code>，产出是 <code>清晰 × 精准</code>。工具的价值不在于它多强，在于它把上下文削得多准。</p>
  </blockquote>
  <p>本页偏工具实践；如果你想先看行业视角和概念框架，见 <a href="/research/context-engineering">上下文工程（行业视角）</a>。</p>

  <h2 id="why-context">为什么上下文工程是 ROI 最高的层</h2>
  <p>在 4 层工具体系里，Context Engineering 通常是最值得优先投入的层。原因：</p>
  <ul>
    <li><strong>改一行配置</strong>（AGENTS.md 加一条规则）就能立刻看到 AI 产出变化</li>
    <li><strong>不需要换工具</strong>，同一套 Cursor/Claude Code 都能受益</li>
    <li><strong>可复用</strong>，项目级配置跨团队成员生效</li>
  </ul>
  <p>相比之下，换个 AI 编码工具（比如 Cursor → Claude Code）的迁移成本远高于优化上下文。</p>

  <h2 id="agents-md">AGENTS.md · 项目说明书</h2>
  <p><strong>状态</strong>：🟢 主流 · <strong>成本</strong>：极低 · <strong>收益</strong>：高</p>

  <h3>是什么</h3>
  <p>放在项目根目录的 Markdown 文件，给 AI 解释“项目是什么、约定是什么、怎么工作”。Cursor、Claude Code、Cline、Aider 等 Agent 型工具通常支持读取类似项目说明文件，但读取规则和优先级因产品而异。</p>

  <p>本节只点它在工具栈里的位置和成本收益判断；具体的 6 段推荐结构、SKILL.md 互补、subdir 范式、生产级反模式、6 个真实仓库实例，详见 <a href="/handbook/agents-md"><strong>AGENTS.md 实践</strong></a>。</p>

  <h2 id="code-graph">CodeGraph · 代码图谱</h2>
  <p><strong>状态</strong>：🟡 新兴 · <strong>成本</strong>：中（需集成）· <strong>收益</strong>：高（跨文件任务）</p>

  <h3>是什么</h3>
  <p>通过 AST 静态分析 + knowledge graph 构建代码关系图，让 AI 能查询"调用链、影响范围、模块依赖"，而不只是 grep 文本。</p>

  <h3>代表工具</h3>
  <table>
    <thead><tr><th>工具</th><th>形态</th><th>特点</th></tr></thead>
    <tbody>
      <tr><td>GitNexus</td><td>开源 MCP server</td><td>本地静态分析 + 图谱，零云依赖</td></tr>
      <tr><td>CodeGraphContext</td><td>VSCode 扩展 + MCP</td><td>Python/JS/TS 支持，可视化</td></tr>
      <tr><td>code-review-graph</td><td>开源</td><td>面向 PR 审查，自动 blast radius</td></tr>
      <tr><td>Sourcegraph Cody/Amp</td><td>商业 + 开源</td><td>大 codebase 优化，企业版深度集成</td></tr>
      <tr><td>Zed AI / Cursor</td><td>内置</td><td>IDE 集成，跨文件编辑辅助</td></tr>
    </tbody>
  </table>

  <h3>解决什么问题</h3>
  <p>AI 编码最大的痛点之一是<strong>跨文件影响范围判断</strong>。改一个函数，不知道哪些调用方会受影响；加一个 feature，不知道该在哪个文件下手。CodeGraph 让 AI 能问：</p>
  <ul>
    <li>"改 <code>validateUser()</code> 会影响哪些文件？"</li>
    <li>"实现订单取消功能，需要改哪些模块？"</li>
    <li>"这个 PR 涉及的所有函数调用链是什么？"</li>
  </ul>

  <h3>实践建议</h3>
  <ul>
    <li>大型 monorepo 优先上 CodeGraph（grep 失效的 codebase）</li>
    <li>PoC 选 GitNexus（开源 + MCP 标准，验证完再考虑商业方案）</li>
    <li>集成 CI，每次 PR 自动更新图谱</li>
  </ul>

  <h2 id="mcp">MCP · Model Context Protocol</h2>
  <p><strong>状态</strong>：🟢 主流 · <strong>成本</strong>：低（标准协议）· <strong>收益</strong>：高（生态）</p>

  <h3>是什么</h3>
  <p>Anthropic 主导的开放协议，让 AI 模型/Agent 通过标准化方式调用外部工具和访问数据源。相当于 AI 时代的 USB-C。</p>

  <h3>为什么关键</h3>
  <ul>
    <li><strong>一次集成，跨工具通用</strong>：MCP server 写一个，Claude Code / Cursor / Cline 都能用</li>
    <li><strong>生态飞轮</strong>：越来越多 SaaS 官方提供 MCP server（GitHub、Notion、Linear、Postgres）</li>
    <li><strong>Google A2A 协议在跟进</strong>，属于值得持续观察的协议方向</li>
  </ul>

  <h3>必装的 MCP Servers（公司内推荐）</h3>
  <table>
    <thead><tr><th>Server</th><th>作用</th></tr></thead>
    <tbody>
      <tr><td><code>github</code></td><td>读 issue / PR / 创建 review comment</td></tr>
      <tr><td><code>postgres</code> / <code>sqlite</code></td><td>查 schema、跑只读查询（生产库慎用）</td></tr>
      <tr><td><code>filesystem</code></td><td>跨目录读文件（受信任环境）</td></tr>
      <tr><td><code>codegraph</code></td><td>代码图谱查询（GitNexus）</td></tr>
      <tr><td><code>linear</code> / <code>jira</code></td><td>读 ticket，更新状态</td></tr>
      <tr><td><code>notion</code> / <code>confluence</code></td><td>读设计文档、ADR</td></tr>
    </tbody>
  </table>

  <h2 id="repo-map">Repo Map · 项目地图</h2>
  <p><strong>状态</strong>：🟢 主流（Cursor 内置）· <strong>成本</strong>：低 · <strong>收益</strong>：中</p>
  <p>Cursor 的招牌功能：自动扫描项目生成"代码地图"——哪些文件重要、哪些是入口、模块间关系。AI 编码前自动加载相关上下文。</p>
  <p>类似工具：Aider 的 <code>--repo-map</code>、Cline 的文件树增强、Claude Code 的 <code>add-dir</code>。</p>

  <h2 id="memory">Memory 系统 · 长期记忆</h2>
  <p><strong>状态</strong>：🟡 新兴 · <strong>成本</strong>：中 · <strong>收益</strong>：高（团队级）</p>

  <h3>个人层 · 跨 session 记忆</h3>
  <p>Claude Code 的 <code>CLAUDE.md</code>、Cursor 的 Memory、OpenClaw 的 <code>MEMORY.md</code>。关键模式：</p>
  <ul>
    <li><strong>每次 session 末尾</strong>自动 append "今天发生了什么、判断、教训"</li>
    <li><strong>新 session 开始时</strong>读 MEMORY.md，避免重复"对齐"</li>
    <li><strong>手动梳理</strong>：每天/每周把日记合并到 MEMORY.md 主文件</li>
  </ul>

  <h3>团队层 · 共享知识</h3>
  <p>不只是个人——团队需要一个共享的"判断力库"：</p>
  <ul>
    <li><strong>决策记录</strong>：为什么选 PostgreSQL 不选 MySQL（ADR）</li>
    <li><strong>经验档案</strong>：上次生产故障的原因、影响和预防措施</li>
    <li><strong>风格约定</strong>：错误处理用 try/except 还是 Result 类型</li>
  </ul>
  <p>工具：Notion + AGENTS.md、Confluence + 自动化同步、GitHub Wiki + mem0 检索。</p>

  <h2 id="context-budget">Context 预算管理</h2>
  <p>模型 context window 不是无限的。即使是 Gemini 1M、Claude 200K，也会被用完。原则：</p>
  <ul>
    <li><strong>贵的代码优先</strong>：核心业务逻辑 > 配置 > 测试 > 文档</li>
    <li><strong>代码切片而非整文件</strong>：用 CodeGraph 切到具体函数而不是丢整个 5000 行文件</li>
    <li><strong>压缩而非堆叠</strong>：定期 compaction（Claude Code 内置）、移除冗余历史</li>
    <li><strong>外部存储</strong>：把全量数据放外部（DB / file），需要时再 query</li>
  </ul>

  <h2 id="tracking">持续追踪业界</h2>

  <h3>关注工具动态</h3>
  <ul>
    <li>GitNexus：<a href="https://github.com/ai-boost/awesome-harness-engineering">awesome-harness-engineering</a> 季度 review</li>
    <li>MCP 生态：<a href="https://modelcontextprotocol.io">modelcontextprotocol.io</a> 官方 server 目录</li>
    <li>AGENTS.md 实践：Cursor / Anthropic 官方 blog</li>
  </ul>

  <h3>新工具评估标准</h3>
  <ol>
    <li>是否解决现有工具不能解决的问题？</li>
    <li>是否支持 MCP / 开放标准？</li>
    <li>是否能在公司安全规范下使用？</li>
    <li>是否有 1+ 公司生产案例？</li>
    <li>切换成本多高？</li>
  </ol>

  <div class="admonition info">
    <div class="admonition-title">📅 维护说明</div>
    <p>本节每月 review 一次，新工具/新版本会被评估并加入或降级。<a href="https://github.com/atbeta/ai-dev-handbook">GitHub</a> 上有变更记录。</p>
  </div>

</MainLayout>
